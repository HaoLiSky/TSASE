
def _tsase_func():
    fname = _plugin_args[0]
    try:
        temp = tsase.io.read_con(fname)
    except:
        temp = ase.io.read(fname)
    if type(temp) is list:
        temp = temp[0]
    traj(temp)

_tsase_func()

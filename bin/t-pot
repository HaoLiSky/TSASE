#!/usr/bin/env python

import sys

import ase
import tsase

import tutil

traj = tutil.pipein()

pot = tutil.getpot(sys.argv[1])

for atoms in traj:
    atoms.set_calculator(pot)
    print atoms.get_potential_energy()



#!/usr/bin/env python

import os
import sys
import gtk
import tsase

if __name__ == "__main__":
    pid = os.fork()
    if pid:
        os._exit(0)
    q = tsase.xyz()
    if len(sys.argv) > 1:
        q.data_read(sys.argv[1])
    gtk.main()

